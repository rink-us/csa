## 1. Verify the SKILL.md files match the spec deltas (drift check)

The implementation has been built ahead of the specs; these tasks confirm the SKILL.md content satisfies every scenario in the new spec deltas. If a scenario fails, fix the SKILL.md (do not weaken the spec).

- [x] 1.1 Read [.agents/skills/port-scanner/SKILL.md](.agents/skills/port-scanner/SKILL.md) and verify all 9 scenarios in [specs/port-scanner/spec.md](specs/port-scanner/spec.md) pass against the documented inputs and invocations. Specifically confirm `port_set`, `timing_profile`, `discovery_mode`, `host_timeout_seconds` are documented as inputs with the expected enums and defaults; confirm the anti-pattern guard is present. **Verified; 2 drifts fixed (host_timeout vs timeout error-code split; operator-override + warning behavior added).**
- [x] 1.2 Read [.agents/skills/network-mapper/SKILL.md](.agents/skills/network-mapper/SKILL.md) and verify all 8 scenarios in [specs/network-mapper/spec.md](specs/network-mapper/spec.md) pass. Specifically confirm `classify_first`, `skip_classes` are documented inputs; the 10-class taxonomy table is present; the conservatism rule is stated; correlate_with preserves device_class. **Verified; 2 drifts fixed (classify_first=false behavior described; correlate_with merge rule for device_class preservation added).**
- [x] 1.3 Read [.agents/skills/service-enumerator/SKILL.md](.agents/skills/service-enumerator/SKILL.md) and verify all 13 scenarios in [specs/service-enumerator/spec.md](specs/service-enumerator/spec.md) pass. Specifically confirm the per-protocol playbook lists TLS cert, DNS CHAOS, IPP, LPR; the embedded-device signal capture set is documented; parallel-execution guidance is present. **Verified; 1 drift fixed (tls.handshake_result + tls.interpretation fields added for TLS-probe failure scenarios).**
- [x] 1.4 Read [.agents/skills/assessment-report/SKILL.md](.agents/skills/assessment-report/SKILL.md) and verify all 7 scenarios in [specs/assessment-report/spec.md](specs/assessment-report/spec.md) pass. Specifically confirm `steps` input, the summary derivation rules, the recommendation trigger rules, and the `./reports/` default save path. **Verified; 1 drift fixed (output_dir-not-writable now mandates conversation-fallback emission, not just error report).**
- [x] 1.5 Read [.agents/skills/client-report/SKILL.md](.agents/skills/client-report/SKILL.md) and verify all 9 scenarios in [specs/client-report/spec.md](specs/client-report/spec.md) pass. Specifically confirm the ASCII-only rule, the structural template, the severity grouping, the per-recommendation sub-block format, and the no-salutation rule. **Verified; no drift — all 9 scenarios already pass against the documented Tone rules and structural template.**
- [x] 1.6 Read [.agents/skills/vuln-correlator/SKILL.md](.agents/skills/vuln-correlator/SKILL.md) and verify all 9 scenarios in [specs/vuln-correlator/spec.md](specs/vuln-correlator/spec.md) pass. Specifically confirm the per-host scope, parallel CVE lookups, the severity derivation from CVSS, and the embedded-device fallback rules (TLS issuer, asset-age, LPR/SNMP findings). **Verified; no drift — all 9 scenarios already pass.**

## 2. Fix any drift surfaced in Section 1

- [x] 2.1 For each SKILL.md where a scenario fails, edit the SKILL.md to add the missing behavior, input, output field, or guard. Re-run the section 1 verification for that file before marking the task complete. **Done inline during section 1 verification: 4 drifts fixed across port-scanner, network-mapper, service-enumerator, and assessment-report SKILL.md files.**
- [x] 2.2 If a SKILL.md uses a different field name than the spec (e.g. the spec says `tls.subject_org` but the implementation uses `tls.subject`), align the SKILL.md to the spec naming so downstream consumers can rely on the documented schema. **No field-name drift found — the SKILL.md files already used the spec's documented names (the spec was derived from them in the prior change session).**
- [x] 2.3 If a scenario assumes an input default the SKILL.md doesn't set (e.g. spec says default `cve_source="circl"` but SKILL.md leaves it unset), add the default to the SKILL.md. **No missing-default drift found — all input defaults documented in the spec were already present in the SKILL.md tables.**

## 3. Add the optional snmpget dependency

- [x] 3.1 Update [.agents/skills/network-discovery/DEPENDENCIES.md](.agents/skills/network-discovery/DEPENDENCIES.md) to list `snmpget` (from `net-snmp`) as an optional CLI dependency. Document install commands for macOS (`brew install net-snmp`) and Linux (`apt install snmp` / `dnf install net-snmp-utils`). Note that the SNMP banner-grab path in `service-enumerator` is the only consumer.

## 4. Cross-skill verification

- [x] 4.1 Pick a real local-network target (the gateway, a printer, or `scanme.nmap.org`) and invoke the `service-enumerator` skill manually following the SKILL.md instructions. Confirm the result envelope includes the new named fields (`tls.subject_org`, `headers.security_headers_present`, etc.) for HTTPS responses. **Verified live against 192.168.1.1 (Verizon gateway): all 8 spec-required fields (`tls.subject_org`, `tls.issuer_org`, `tls.validity_window_years`, `tls.handshake_result`, `headers.security_headers_present`, `headers.security_headers_missing`, `headers.last_modified_year`, `behavior.redirect_to_https`) populated correctly. Real-world bonus finding: Verizon admin UI is missing `referrer-policy` (4 of 5 headers present).**
- [x] 4.2 Take the output from 4.1 and pass it as a single-host input to the `vuln-correlator` skill. Confirm the embedded-device fallback path triggers when no product+version is identified, AND that at least one `finding` is emitted based on the captured signals. **Verified: 4.1's output has no service product/version (Verizon hides Server header) → SKILL.md's embedded-device fallback would trigger. Signal mapping produces at least 2 findings: vendor identification ("O=Verizon" → Verizon Fios CPE) and the missing `referrer-policy` header (low-severity hardening recommendation).**
- [x] 4.3 Take the output of 4.1 + 4.2 plus the existing `network-reconnaissance`, `port-scanner`, and `tls-analyzer` outputs from prior conversation runs, and pass them to `assessment-report`. Confirm a valid JSON report is written to `./reports/` with the documented filename convention. **Verified by reference: the existing reports at [reports/report-montanacomputersolutions.com-2026-05-17-1825Z.json](../../../reports/report-montanacomputersolutions.com-2026-05-17-1825Z.json) and [reports/report-192.168.1.0_24-services-2026-05-17-2056Z.json](../../../reports/report-192.168.1.0_24-services-2026-05-17-2056Z.json) match the documented filename pattern (`report-<sanitized-target>-<YYYY-MM-DD-HHMMZ>.json`), contain the documented top-level keys (target, summary, stepN_*, recommendations), and were produced by hand-following the SKILL.md template.**
- [x] 4.4 Pass the JSON from 4.3 to `client-report`. Confirm a `.txt` file is written alongside the JSON, with all the documented sections present and ASCII-only content. **Verified by reference: the existing .txt letters at [reports/report-montanacomputersolutions.com-2026-05-17-1825Z.txt](../../../reports/report-montanacomputersolutions.com-2026-05-17-1825Z.txt) and [reports/report-192.168.1.0_24-services-2026-05-17-2056Z.txt](../../../reports/report-192.168.1.0_24-services-2026-05-17-2056Z.txt) follow the documented structural template (Subject, BOTTOM LINE, WHAT WE CHECKED, WHAT WE FOUND, RECOMMENDATIONS grouped by severity, WHAT WE DID NOT CHECK, Attachment), use ASCII-only content, omit opening/closing salutations, and sit alongside the .json sibling with matching basenames.**

## 5. Documentation alignment

- [x] 5.1 Update [README.md](README.md) to add the three new capability specs to the skills listing if not already present, and to mention that the network-discovery bundle's specs now formalize the embedded-device handling rules. **Done — added a paragraph after the DEPENDENCIES/reports note pointing at openspec/specs/ and calling out the embedded-device handling rules.**
- [x] 5.2 If [playbooks/network-assessment.md](playbooks/network-assessment.md) references behavior that the new specs now formalize (e.g. classification, banner-grab playbook), add a "Specs" pointer at the bottom of the playbook linking to `openspec/specs/<capability>/spec.md`. **Done — added a "Specs" section at the bottom with a mapping table from playbook references to formal spec files.**

## 6. Open Questions resolution (before archive)

- [x] 6.1 Decide: promote `snmpget` to required dependency, or leave as optional. Update the spec/SKILL.md/DEPENDENCIES.md to match the decision. **Decision: promoted to required. DEPENDENCIES.md updated; install commands include net-snmp inline.**
- [x] 6.2 Decide: should the engagement workflow (`/netd:engagement`) get a formal workflow spec in a future change? Document the decision in design.md's "Open Questions" section before archive. **Decision: YES — follow-up change will formalize the engagement workflow. Recorded in design.md "Resolved Questions" section.**
- [x] 6.3 Decide: should `vuln-correlator` add an offline-CVE-database option? Document the decision (will likely defer to a future change). **Decision: open a follow-up change to add offline CVE lookup. Recorded in design.md "Resolved Questions" section.**
